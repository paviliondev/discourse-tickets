en:
  site_settings:
    errors:
      tickets_enabled_requires_tagging: "Tagging has to be enabled to enable tickets."
    tickets_enabled: "Enable tickets on topics? (requires tagging to be enabled)."
    tickets_icon: "FontAwesome class of tickets icon."
    tickets_include_group: "Name of group to include on all private messages with tickets."
